<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff"
    tools:context=".FingurPrintAuthActivity">

    <TextView
        android:id="@+id/Fingurprint_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:layout_marginTop="50dp"
        android:gravity="center"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:text="Scan Fingerprint" />

    <ImageView
        android:id="@+id/Fingurprint_img"
        android:layout_below="@id/Fingurprint_header"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        app:srcCompat="@mipmap/ic_launcher_fingurprint" />

    <TextView
        android:id="@+id/Fingurprint_text"
        android:layout_width="match_parent"
        android:layout_below="@id/Fingurprint_img"
        android:layout_height="wrap_content"
        android:textSize="22sp"
        android:layout_margin="15dp"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:textStyle="italic"
        android:text="Plase place your finger on scanner" />
</RelativeLayout>